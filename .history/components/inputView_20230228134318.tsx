import { StyleSheet, View } from "react-native";

const inputView = () => {
    return(
        <View>

        </View>
    )
}

StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: 'center'
    }
})

export default inputView;