import { Text, View, StyleSheet } from "react-native";

const MainScreen = () => {
    return (
        <View>
            <Text>
                commentça va ? 
            </Text>
        </View>
    )
};

export default MainScreen;